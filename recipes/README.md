# Horizon recipes

| Recipe | Purpose |
|--------|---------|
| `compliance-trustlint.yaml` | EU AI Act offline gate (ComplyEdge TrustLint) on LLM-facing intent + agent rules |

Load the CE agent block from `recipes/compliance-trustlint.yaml` into `docs/cursor-rules/horizon-monitor.mdc`.

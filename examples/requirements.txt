# Dependencies to run everything under examples/.
#
# `horizon-monitor` itself is NOT listed here — install it from the repo
# root first (`pip install -e .` or `pip install -r requirements-dev.txt`).
#
# examples/raw_framework_agnostic_e2e.py needs nothing beyond horizon-monitor
# and the stdlib — it is the zero-key, fully offline starting point.

# Used by every example except raw_framework_agnostic_e2e.py, to load
# OPENAI_API_KEY / ANTHROPIC_API_KEY from a local .env file.
python-dotenv>=1.0.0

# examples/openai_real_agent_e2e.py
openai>=1.0.0

# examples/anthropic_real_agent_e2e.py
anthropic>=0.25.0

# examples/langchain_real_agent_e2e.py
langchain-core>=0.2.0
langchain-openai>=0.1.0

# examples/openai_agents_sdk_e2e.py (imports the `agents` package)
openai-agents>=0.0.3
